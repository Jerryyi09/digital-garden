# Standalone HTML

Put already-generated HTML pages here.

Examples:

- `html/demo/index.html` becomes `/html/demo/`
- `html/report.html` becomes `/html/report.html`

This folder is copied into `public/html` after Quartz finishes building.
